meta {
  name: create_data_chef_from_sql
  type: http
  seq: 2
}

post {
  url: http://localhost:9999/api/v1/create_data_chef_from_sql
  body: json
  auth: inherit
}

body:json {
  {
    "data_chef_id": "sql_interactions",
    "query": "SELECT user_id, item_id, rating FROM interactions",
    "connection_string": "mysql://user:pass@localhost:3306/db"
  }
}

settings {
  encodeUrl: true
}
