---
# Server configuration
server:
    host: "0.0.0.0"
    port: 2030
    ai_server_url: "http://localhost:9999"
    # Frontend URL - Can be overridden by FRONTEND_URL environment variable
    # For DDNS/LAN access, set FRONTEND_URL in .env file
    frontend_url: "http://localhost:5173"

# Logger configuration
logger:
    max_size: 10
    max_backups: 5
    max_age: 30
    compression: true
    local_time: true

# JWT configuration
jwt:
    secret_key: "your_secret"
    expire_mins_count: 1440
    refresh_key: "refresh_secret"
    refresh_expire_hours_count: 720

# Database configuration for email whitelist
database:
    # Database type: "mysql" or "postgresql"
    type: "mysql"
    host: "192.168.2.12"
    port: 3306
    user: "admin"
    password: "pokiwar0981"
    db: "shop"
    ssl: false
    max_idle_conns: 5
    max_open_conns: 25
    conn_max_lifetime: 300 # seconds

# Redis configuration
redis:
    host: "redis-11030.c1.ap-southeast-1-1.ec2.redns.redis-cloud.com"
    port: 11030
    password: "UqIklPGg9na7naIGGTTlvrIXqWPsTJSH"
    db: 0
    ssl: false
    max_retries: 3
    pool_size: 10
    min_idle_conns: 5
    max_conn_age: 0
    pool_timeout: 4
    idle_timeout: 300
    read_timeout: 3
    write_timeout: 3
