# System configuration
# Environment can be 'dev', 'test', 'prod'
STAGE_STATUS=dev

# Server configuration for AI server
SERVER_TITLE=AI Server
SERVER_VERSION=1.0.0
SERVER_DESCRIPTION=A server for AI model management and inference
SERVER_HOST=0.0.0.0
SERVER_PORT=8900

# Logger configuration
LOG_MAX_SIZE=10
LOG_MAX_BACKUPS=5
LOG_MAX_AGE=30
LOG_COMPRESSION=true
LOG_LOCAL_TIME=true

# Middleware configuration
MIDDLEWARE_ALLOW_ORIGIN=*
MIDDLEWARE_ALLOW_CREDENTIALS=true
MIDDLEWARE_ALLOW_METHODS=GET, POST, PUT, DELETE, OPTIONS
MIDDLEWARE_ALLOW_HEADERS=Content-Type, Authorization, X-Requested-With

# Database configuration
# Database type can be 'mysql', 'postgres'
DATABASE_TYPE=mysql
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USER=root
DATABASE_PASSWORD=pokiwar0981
DATABASE_NAME=shop
DATABASE_SSL_MODE="disable"
DATABASE_MAX_OPEN_CONNS=100
DATABASE_MAX_IDLE_CONNS=10
DATABASE_CONN_MAX_LIFETIME=30
DATABASE_CONN_MAX_IDLE_TIME=30

# Redis configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_USERNAME=user
REDIS_PASSWORD=pokiwar0981
REDIS_SSL=false
REDIS_DECODE=false

# Kafka configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPICS=topic1,topic2

# Spark configuration 
SPARK_MASTER=spark://localhost:7077
SPARK_APP_NAME=AIApp
